{
  "name": "volunteer-database",
  "version": "0.1.0",
  "private": true,
  "description": "Esquema de dados do protótipo do Sistema de Voluntários",
  "scripts": {
    "test": "node --test",
    "db:validate": "prisma validate --schema prisma/schema.prisma",
    "db:format": "prisma format --schema prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.16.0"
  }
}
